name: Build Packages

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.arch }}-${{ matrix.tag }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64
        tag:
          - openwrt-24.10
          - openwrt-25.12

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up ARCH
        run: |
          echo "EFFECTIVE_ARCH=${{ matrix.arch }}" >> $GITHUB_ENV
          echo "ARCH=${{ matrix.arch }}-${{ matrix.tag }}" >> $GITHUB_ENV

      - name: Build Packages
        uses: sbwml/openwrt-gh-action-sdk@go1.26
        env:
          FEEDNAME: packages_ci
          PACKAGES: luci-app-quickfile
          NO_REFRESH_CHECK: true

      - name: Create compress files
        run: |
          tar -zcvf "$ARCH.tar.gz" -C "bin/packages/$EFFECTIVE_ARCH/" packages_ci

      - name: Upload packages
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ env.ARCH }}.tar.gz
          name: ${{ env.ARCH }}.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
          allowUpdates: true
          replacesArtifacts: true
          prerelease: false
          artifacts: "${{ env.ARCH }}.tar.gz"
